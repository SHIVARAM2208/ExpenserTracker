{
  "name": "ExpenseTrackermain",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mongoose": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "bcrypt": "^5.1.0"
  }
}
